import * as azdata from 'azdata';
import * as vscode from 'vscode';
import { StubComponent } from './stubComponent';
export declare class StubRadioButton extends StubComponent implements azdata.RadioButtonComponent {
    click(): void;
    readonly id = "radio-button";
    private _onDidClickEmitter;
    private _onDidChangeCheckedStateEmitter;
    onDidClick: vscode.Event<any>;
    onDidChangeCheckedState: vscode.Event<boolean>;
    label?: string;
    value?: string;
    checked?: boolean;
}
